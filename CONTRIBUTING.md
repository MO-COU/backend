# 협업 규칙

MO-COU 백엔드 팀의 공통 작업 규칙이다.

## 작업 흐름

1. **이슈 생성** — 모든 작업은 이슈에서 시작한다.
2. **브랜치 생성** — `dev`에서 브랜치를 따서 작업한다.
3. **커밋** — 의미 단위로 쪼개서 커밋한다.
4. **PR 생성** — `dev`로 PR을 올리고 리뷰어를 지정한다.

> 'dev'에 개인 작업을 PR하도록 한다.
> `main`은 항상 배포 가능한 상태를 유지한다. 직접 push 하지 않는다.

## 이슈 규칙

작업을 시작하기 전에 이슈를 먼저 만든다. 템플릿은 하나이고, 작업 종류는 라벨로 구분한다.

| 라벨 | 용도 |
| --- | --- |
| `feature` | 새로 만들 기능 |
| `bug` | 동작하지 않는 부분 |
| `refactor` | 동작 변화 없는 구조 개선 |
| `test` | 테스트 코드 추가·수정 |
| `docs` | 문서 작업 |
| `chore` | 빌드, 설정, 의존성 |

- **이슈를 만든 뒤 사이드바에서 라벨을 직접 지정한다.** 여기서 고른 라벨이 그대로 브랜치 타입과 커밋 타입이 된다.
- 제목은 무엇을 하는지 알 수 있게 쓴다. (`카카오 소셜 로그인 구현`)
- 하나의 이슈는 1~2일 안에 끝낼 수 있는 크기로 자른다. 커지면 쪼갠다.
- `할 일`은 최대한 세분화하고, 작업하면서 체크해 나간다. 팀원이 물어보지 않아도 진행 상황을 알 수 있다.
- 담당자(Assignees)는 작업을 시작할 때 지정한다.
- 이슈는 직접 닫지 않는다. PR 본문의 `Closes #12`로 머지와 함께 닫힌다.

## 브랜치 규칙

```
<타입>/<이슈번호>-<간단한-작업명>
```

| 예시 | 설명 |
| --- | --- |
| `feature/12-signup-api` | 기능 개발 |
| `fix/15-token-expire` | 버그 수정 |
| `refactor/20-user-service` | 리팩토링 |
| `chore/3-gradle-setup` | 설정, 빌드, 문서 등 |

- 작업명은 영문 소문자 + 하이픈으로 쓴다.
- 머지된 브랜치는 바로 삭제한다.

## 커밋 메시지

```
<타입>: <내용>
```

| 타입 | 사용할 때 |
| --- | --- |
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 기능 변화 없는 코드 구조 개선 |
| `test` | 테스트 코드 추가/수정 |
| `docs` | 문서 수정 |
| `chore` | 빌드, 설정, 의존성 등 그 외 |

**예시**

```
feat: 회원가입 API 구현
fix: 만료된 토큰 재발급 시 500 에러 수정
chore: Spring Security 의존성 추가
```

## PR 규칙

- **제목**: 커밋 메시지와 동일한 형식 (`feat: 회원가입 API 구현`)
- **크기**: 변경 파일 10개 / 300줄 넘어가면 쪼갤 수 있는지 한 번 고민한다. 리뷰가 안 되는 PR은 통과 도장만 찍힌다.
- **연결**: 본문에 `Closes #12`를 적으면 머지 시 이슈가 자동으로 닫힌다.
- **WIP**: 아직 리뷰받을 준비가 안 됐으면 Draft PR로 올린다.

## 주의사항

- **개인정보를 저장소에 올리지 않는다.** 실제 회원 데이터, 연락처, 테스트용 계정 정보 등을 코드나 이슈·PR 본문에 붙여넣지 않는다.
- **시크릿을 커밋에 포함하지 않는다.** DB 접속 정보, API 키, JWT 시크릿 등은 `.env`나 `application-local.yml`로 분리하고 `.gitignore`에 등록한다. 저장소에는 키 이름만 담은 예시 파일(`.env.example`)을 올린다.
- **실수로 올렸다면 즉시 팀에 알리고 해당 키를 재발급한다.** 커밋을 되돌려도 히스토리와 GitHub 캐시에 남기 때문에, 키 폐기 외에는 방법이 없다.
- 남의 브랜치에 push 하지 않는다.
- 막혀서 진도가 안 나가면 혼자 붙들고 있지 말고 팀 채널에 공유한다.

## 패키지 소유권 규칙

`src/main/java/com/mocou` 아래 패키지는 팀별로 나뉘어 있다.  논의 후에 변경될 수 있음.

| 패키지 | 담당 |
| --- | --- |
| `global`, `member`, `coupon` | 공통 (읽기 전용으로 참조만) |
| `issue` | A팀 |
| `lifecycle`, `consistency`, `datagen` | B팀 |
| `admin`, `notification` | C팀 |

**다른 팀의 패키지를 직접 import하지 않는다.** 데이터가 필요하면:
1. 공통 패키지(`member`, `coupon`)에 있는 것만 참조하거나
2. 상대 팀에 API 스펙을 요청해서 HTTP/이벤트로 받아온다.

이 규칙을 지키면 팀별 작업이 충돌 없이 병렬로 진행된다.

## 로컬 개발 환경

```bash
docker compose up -d          # MySQL, Redis 기동
./gradlew bootRun # --args='--spring.profiles.active=local' 
```

## 테스트

- 단위 테스트: Mockito 등으로 순수 로직 검증
- 통합 테스트: Testcontainers로 실제 MySQL/Redis 컨테이너 사용 (DB/Redis Mock 금지)
- 예외: 알림(`notification`)은 설계상 외부 API를 Mock으로 대체하므로, Mock 발송 결과가 정상 기록되는지만 검증한다
